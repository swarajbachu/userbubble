/**
 * <UserbubbleWidget /> — renders nothing.
 * The widget UI lives in Shadow DOM, managed by the SDK.
 * This component exists so React users have a familiar mount point.
 */
export function UserbubbleWidget() {
  return null;
}
