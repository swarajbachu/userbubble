{
  "name": "create-t3-turbo",
  "private": true,
  "engines": {
    "node": "^23.10.0",
    "pnpm": "^10.19.0"
  },
  "packageManager": "pnpm@10.19.0",
  "scripts": {
    "build": "turbo run build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo run clean",
    "auth:generate": "pnpm -F @critichut/auth generate",
    "db:generate": "pnpm -F @critichut/db generate",
    "db:migrate": "pnpm -F @critichut/db migrate",
    "db:push": "turbo -F @critichut/db push",
    "db:studio": "turbo -F @critichut/db studio",
    "dev": "turbo watch dev --continue",
    "dev:next": "turbo watch dev -F @critichut/nextjs...",
    "typecheck": "turbo run typecheck",
    "format": "ultracite fix .",
    "lint": "ultracite lint .",
    "ui-add": "pnpm -F @critichut/ui ui-add",
    "android": "expo run:android",
    "ios": "expo run:ios"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.4",
    "@turbo/gen": "^2.5.8",
    "dotenv-cli": "^10.0.0",
    "lefthook": "^2.0.2",
    "turbo": "^2.5.8",
    "typescript": "catalog:",
    "ultracite": "6.3.2"
  }
}
