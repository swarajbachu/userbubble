# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# Database URL — local Docker Postgres (run `docker compose up -d`)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/userbubble"

# Production: Supabase pooler URL
# DATABASE_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"


# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='supersecret'

# Preconfigured Google OAuth provider, works out-of-the-box
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''

# Base domain for subdomain detection (e.g., userbubble.vercel.app)
# Used to properly detect organization subdomains in production
NEXT_PUBLIC_BASE_DOMAIN='userbubble.vercel.app'

# Encryption key for API keys stored in the database (AES-256)
# Generate via: openssl rand -hex 32
ENCRYPTION_KEY=''

# Modal worker webhook for AI PR generation
MODAL_WEBHOOK_URL=''
MODAL_WEBHOOK_SECRET=''

# Optional: OpenAI Codex OAuth client ID (defaults to public Codex client ID)
OPENAI_CODEX_CLIENT_ID=''